pipeline {
  agent { label 'master' }
  stages {
    stage('test') { 
      environment{
        name = sh(script: '''curl GET http://54.244.111.147:8080/crumbIssuer/api/json --user admin:admin|awk  -F , '{print $2}'  ''', returnStdout: true)        
      }
      steps {
        sh ' echo $name '
      }
    }
    
    }
  }
