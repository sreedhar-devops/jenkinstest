pipeline {
  agent { label 'master' }
  stages {
    stage('test') { 
      when {
        expression { value == 'Rosy'}
      }    
      environment{
        value = "${value}"
           } 
      steps {
        sh ' echo $value '
      }
    }
    
    }
  }
